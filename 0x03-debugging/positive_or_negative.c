#include "main.h"

/**
 * postitive_or_negative - checks if the number is positive
 * or negative
 */
void positive_or_negative(int i)
{
	if (i > 0)
		printf("%d is greater than zero\n", i);
	
	else if (i > 0)
		printf("%d is less than zero\n", i);
	
	else
		printf("%d is zero\n", i);
}
